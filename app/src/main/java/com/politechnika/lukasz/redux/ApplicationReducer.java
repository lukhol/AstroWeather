package com.politechnika.lukasz.redux;

public class ApplicationReducer {
    /*
    public BiFunction<ApplicationState, Object, ApplicationState> applicationReducer = (lastState, action) ->{
        return new ApplicationState(lastState.getCounter());
    };

    public static ApplicationState reducer(ApplicationState lastState, Object action){
        return new ApplicationState(lastState.getCounter());
    }
    */
}
